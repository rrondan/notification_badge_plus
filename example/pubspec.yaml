name: notification_badge_plus_example
description: Example app for the notification_badge_plus Flutter plugin.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  notification_badge_plus:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true